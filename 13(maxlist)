/* Program to find the maximum element in a list */

/* Base case: maximum of a single-element list */
maxlist([X], X).

/* Recursive case: compare head with max of tail */
maxlist([H | T], M) :-
    maxlist(T, M1),
    ( H > M1 -> M = H ; M = M1 ).
